```python
git add .	# 提交当前目录下所有变更和新增文件
# (建议使用如下git add)
git add -p
git commit -m 'init commit'		# commit
# (建议使用如下git commit)
git commit 进入编辑信息的模式提交代码
git checkout -b add-blog-app-model	# 新建分支
```

# 分支
```bash
git branch --list
git branch --all
git branch --remotes

git fetch

git checkout --track origin/video-lessons
git checkout --track -b video-lessons origin/video-lessons

```

# 在仓库中添加更改
```bash

```